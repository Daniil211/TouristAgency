@using System.Text.RegularExpressions

<div class="col-sm-12 col-md-4 p-3 text-center">
    <div class="tour-name">
        <a href="@url">@Game.TourName</a>
    </div>

    <hr class="hrfix" />

    <div class="card-image">
        <a href="@url">
            @if (IsBase64(Game.Image))
            {
                <img src="data:image/jpeg;base64,@Game.Image" class="tour-image" />
            }
            else
            {
                <img src="@Game.Image" alt="@Game.TourName" class="tour-image" />
            }
        </a>
    </div>
    <hr class="hrfix" />

    <AuthorizeView Roles="Admin">
        <NotAuthorized>

        </NotAuthorized>
        <Authorized>
            <div class="tour-card-footer">
                <div class="btn-group my-3 text-center" role="group">
                    <button type="button" class="btn btn-outline-info" @onclick="(() => OnEditGame.InvokeAsync(Game))">
                        <i class="fas fa-edit"></i>
                    </button>
                    <button type="button" class="btn btn-outline-danger" @onclick="(() => OnDeleteGame.InvokeAsync(Game))">
                        <i class="fas fa-trash-alt"></i>
                    </button>
                </div>
            </div>
        </Authorized>
    </AuthorizeView>
</div>

@code {

    [Parameter]
    public Tour Game { get; set; }
    [Parameter]
    public EventCallback<Tour> OnDeleteGame { get; set; }
    [Parameter]
    public EventCallback<Tour> OnEditGame { get; set; }

    string url = string.Empty;

    protected override void OnInitialized()
    {
        //url = $"games/{Game.Id}/{Game.Name.Replace(" ", "-")}";
        url = $"tours/{Game.TourId}/{Uri.EscapeDataString(Game.TourName)}";
    }

    bool IsBase64(string value)
    {
        value = value.Trim();
        return ((value.Length % 4 == 0) && Regex.IsMatch(value, @"^[a-zA-Z0-9\+/]*={0,3}$", RegexOptions.None));
    }
}
