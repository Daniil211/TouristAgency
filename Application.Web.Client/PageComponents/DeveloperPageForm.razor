@using Application.Database.Models
<EditForm Model="Developer" OnValidSubmit="OnValidSubmitHandler">
    <div class="form-group">
        <label>Operator Name: </label>
        <div>
            <InputText class="form-control" @bind-Value="Developer.Fio"/>
            <ValidationMessage For="(()=>Developer.Fio)"/>
        </div>
    </div>
    <div class="form-group">
        <label>Age: </label>
        <div>
            <Input class="form-control" @bind-Value="Developer.Age"/>
            <ValidationMessage For="(()=>Developer.Age)"/>
        </div>
    </div>
    
    <InputImage Label="Image" OnImageFileSelected="ImageFileSelectedHandler" ImageUrl="@imageUrl"/>
    
    <div class="form-group form-markdown">
        <MarkdownInput @bind-Value="Developer.Resume" Label="Resume" OnFor="(() => Developer.Resume)" />
    </div>

    <button type="submit" class="btn btn-primary">Add New Tour Operator</button>
</EditForm>

@code {
    [Parameter]
    public TourOperator Developer { get; set; }

    [Parameter]
    public EventCallback OnValidSubmitHandler { get; set; }

    string imageUrl;

    protected override void OnInitialized()
    {
        imageUrl = Developer.Image;
        Developer.Image = null;
    }

    void ImageFileSelectedHandler(string imageBase64)
    {
        Developer.Image = imageBase64;
        imageUrl = null;
    }
}
